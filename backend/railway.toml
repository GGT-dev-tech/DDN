[build]
builder = "docker"
dockerfilePath = "Dockerfile.prod"

[deploy]
startCommand = "uv run uvicorn apps.api_gateway.src.main:app --host 0.0.0.0 --port $PORT --workers 4"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
